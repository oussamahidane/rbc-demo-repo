# Progress Log

Append-only, newest last. One entry per session: date — what shipped, decisions made, next single action.

---

- **2026-07-12** — Campaign brief created from planning chat. Next action: mentee Call 1 + resume tailoring.
- **2026-07-12** — Workspace scaffolding shipped: `CLAUDE.md` (session rules), `memory.md` (living state), `progress.md` (this log). Progress Log moved out of the game plan; game plan now redirects here. Next action: mentee Call 1 + resume tailoring.
- **2026-07-12** — Sequencing confirmed: Call 1 → portal submission → intro ask → meetings; backstop rule added (Jul 15 text / Jul 18 submit regardless). Shipped `call-1-brief.md`; revised after note that mentee already knows the full story — call is pure intel, pivot line now seeds the locked frame to overwrite his retelling. Sequence locked: contact → apply (Jul 14–16) → intro ask → meetings. Next action: Oussama books Call 1 + drops resume in workspace.
- **2026-07-12** — **Workstream A: resume + cover letter v1 shipped** (docx, one page each, render-verified). Posting verified LIVE — official title "AI Analyst", Req R-0000179708, deadline confirmed (submit by Jul 22 23:59 ET); exact JD + keyword bank captured to `jd-rbc-ai-analyst.md` (earlier "filled" page was a stale URL variant — false alarm). Decisions: Toronto header, "Dear Hiring Team", MSc shown in-progress without dates. Letter carries locked frame verbatim + compressed token story + comp-alignment line. Next action: Oussama reviews both drafts; letter date needs updating to actual submission day.
- **2026-07-12** — Resume + letter v2: stripped all em dashes and AI-telltale phrasing per Oussama; style rule locked into `CLAUDE.md` and `memory.md` (applies to blurb and deck too). Both docs re-verified: one page each, zero em dashes.
- **2026-07-12** — **Deck content v1 shipped** (`deck-content.md`, md only per Oussama; pptx build waits for his go). 10 slides per game plan §7: hypotheses for correction → proof → operating model → JD side-by-side (exact posting quotes) → first 90 days → demo → eval harness (own slide, the differentiator) → leave-behind → single ask. `[INTEL]` slots on slides 2/6/8 wait on Call 1; JD quotes pulled from `jd-rbc-ai-analyst.md`; demo/eval slides reference the real analyzer artifacts (MASTER-PROMPT.md, design-system.md, 3/3 scorecard). Slide 4 rewritten per Oussama: the operating model is his lived founder loop (daily brainstorm → evaluate first → decision-ready proposal: problem, approach, people and capital required, key commit/don't-commit factors), punchline "I won't put more work on my manager's plate. I'll take work off it." Next action: Oussama reviews deck content; Call 1 intel then upgrades slides 2/6/8.
- **2026-07-12** — **Deck built to pptx** (`Deck - AI Adoption - Oussama Hidane.pptx`): 10 slides, 16:9, navy #1A365D brand styling, no RBC marks, dark title/ask bookends, JD table verbatim, $5 and 3/3 stat callouts, speaker notes carry presenting guidance + `[INTEL]` reminders privately. File validated, all 10 slides visually QA'd (slide 4 title shortened to fix a wrap). Slide 3 row B revised per Oussama: leads with the AI stack he runs (prompt and context engineering, Claude API, MCP servers, agents and skills), pipeline and ERP integration demoted to proof points; wording mirrors the JD's "agents, systems and skills". Next action: Oussama reviews deck; slides 2/6/8 get upgraded after Call 1.
- **2026-07-12** — Factual corrections from Oussama on both degree names: HEC = **MSc Finance** (not Quantitative Finance), INCEIF = **Master in Islamic Finance** (not Islamic Accounting & Finance). Game plan §3 had both wrong; fixed there, in memory.md, deck-content.md, and across resume, cover letter and deck; all three artifacts regenerated, validated, verified (resume still one page). Quant skills stay claimed via coursework and CFA, not the degree name.
- **2026-07-12** — **Workstream C (build): earnings-call analyzer shipped end-to-end** (parallel to Cowork on resume/deck). Self-prompt written to `workstream-c-build-brief.md`. Tool lives in `earnings-call-analyzer/`: ingest → analyze (deterministic `--mock` + real Claude behind `ANTHROPIC_API_KEY`) → grounding layer (every figure cites `[Ln]`; financial figures verified against source) → house-style report rendered from `design-system.md` → eval harness with 3 synthetic golden transcripts. **Verified offline:** `python -m eca --mock eval` PASSES 3/3 at 100% accuracy / 100% grounding / 100% numeric / 100% consistency; tone read differentiates (Atlas net-negative, Meridian/Northwind net-positive). Docs: `README.md`, `MASTER-PROMPT.md` (reproducibility deliverable), `docs/methodology.md` (LM/FinBERT grounding, cited), `docs/build-log.md`. Sample reports committed under `reports/`. **Decisions:** mock-first so evals/demo run free & offline; golden set synthetic (needs known ground truth + dodges copyright); default model `claude-sonnet-5`. Next action: verify real-Claude path with an API key, pick the live-demo transcript (mega-cap), record fallback demo video.
- **2026-07-12** — **Workstream C (build): web interface shipped** for the analyzer, in `earnings-call-analyzer/web/`. Cloudflare **Pages + Functions** (Workers runtime, same-origin so no CORS), for the manager demo at **rbc.oussamahidane.com**. Pipeline ported to TypeScript (`web/src/`, mirrors the Python `eca` package; Python stays the reference + eval harness). API `functions/api/analyze.ts`: transcript in → grounded report out; real Claude via `fetch` behind `ANTHROPIC_API_KEY` (server-side, never in client), deterministic **sample** backend when no key. Password gate `functions/_middleware.ts` (HTTP Basic Auth vs `APP_PASSWORD`, gates UI **and** API). UI (`public/`): navy/gold, no RBC logos, MNPI banner, CSP locked to 'self', citation chips that **hover to show the source line**, scorecard, download-report, footer "independent demo, not affiliated with RBC". **Verified locally (`wrangler pages dev`):** typecheck clean; `npm test` 4/4 (TS port matches Python on all golden transcripts); API 100% grounding on 24 claims; browser end-to-end render; access gate 401/401/200 (+API 401). **Decisions:** TS Worker+Pages; shared-password gate; domain already on Cloudflare (all per Oussama). Stripped em dashes from all user-visible UI/report strings per the locked style rule. **Not done (needs Oussama + key):** real-Claude verify with a live key, `wrangler pages deploy` + `wrangler pages secret put` (ANTHROPIC_API_KEY, APP_PASSWORD) + custom-domain wiring — runbook in `web/README-web.md`. Next action: Oussama deploys and sets secrets, then smoke-tests at the domain.
- **2026-07-12** — **DEPLOYED LIVE.** Workspace pushed to github.com/oussamahidane/rbc-demo-repo; Cloudflare Pages connected via git (project `eca-web`, root `earnings-call-analyzer/web`, output `public`). Secrets set by Oussama in dashboard: ANTHROPIC_API_KEY + APP_PASSWORD (`rbcAI@2026`). Model bumped to **claude-opus-4-8** via wrangler.jsonc vars. **$5/week/IP cost cap** implemented: KV namespace `eca-web-RATE_LIMIT_KV` (id e4cbe262...) created + bound; middleware checks weekly spend per CF-Connecting-IP and 429s over budget; analyze endpoint estimates Opus token cost per request and accumulates in KV (7-day TTL). Custom domain **rbc.oussamahidane.com** wired. MASTER-PROMPT updated with web-UI note. Shipped `WHAT-THIS-PROJECT-DEMONSTRATES.md` (root): the AI-at-every-step process report (brainstorm → chat plan → md workspace/context engineering → parallel sessions → eval-first build → verified deploy → JD mapping → human/AI division), zero em dashes. Next action: Oussama smoke-tests live site with real key (report + rate-limit behavior), then picks live-demo transcript + records fallback video.
- **2026-07-12** — **Web UI: How-it-works + How-it-was-built pages shipped** (static HTML, shared stylesheet, header nav on all three pages, clean URLs, covered by the existing password gate). Framing corrected before build per Oussama's invite: engine wraps Claude (5 stages, model is 1 of 5), TS port with Python reference + parity tests (NOT "Python runs the web app"), sentiment = LM lexicon in code for sample mode / constrained Claude read in live mode, literature baseline not novel. Verified locally (curl 200s + browser render), pushed 4c27d04, auto-deploy live. Next action: hard-refresh rbc.oussamahidane.com and click through the nav.
- **2026-07-12** — **Deck rebuilt to v2.** Slide 2 reframed from four flat pain points to four objective-cards: headline is the team objective (AI reaches every desk / outputs they trust with real money / a quality bar that holds up / wins that compound across desks), body is the concrete daily job that serves it, plus a framing subtitle; the exact posting line behind each card lives in speaker notes. Those four objectives now thread slides 4/5/6/8/9 (mostly in notes) so the deck argues one case. Two factual fixes: slide 1 subtitle dropped the unverified "AI for Alpha initiative" (softened, restore only if confirmed real), slide 7 pipeline dropped "audio" (tool ingests PDF/transcript only). Slide 5 retitled "Your posting, matched". Full proposed content shipped to `deck-content-v2.md` and reviewed before build. Deck edited in place via python-pptx (bd.js was lost to the scratchpad clear; the edit script is also transient, so future edits inspect-then-patch the pptx). Validated: 10 slides, all visual elements intact, every slide re-rendered and QA'd. Next action: Oussama confirms whether "AI for Alpha" is a real initiative and that the four objective headlines match his read; Call 1 intel then sharpens slides 2/6/8.
- **2026-07-12** — **Google Vids voiceover script shipped + video deck built.** Wrote a per-slide first-person narration (about 5.5 min) to `deck-video-script.md`, split into spoken voiceover (for notes) vs Vids build direction (not spoken). Built a separate `Deck - AI Adoption - Oussama Hidane - VIDEO.pptx` whose speaker notes carry ONLY the clean narration, so Google Vids reads exactly the script; the main presentation deck keeps its live-delivery notes and [INTEL] reminders untouched. Scene 7 calls for a 20-30s screen recording of the analyzer at rbc.oussamahidane.com (the key media). Choices Oussama left open: AI voice vs own voice; first person (kept) vs narrator; whether to also overwrite the main deck's notes (not done, kept the video separate). Next action: Oussama records the demo clip, imports the VIDEO deck into Google Vids, generates the voiceover, exports 16:9.
- **2026-07-12** — **Added slide 9 "How this was built."** New stat-box slide after the evals slide (mirrors the $5 and 3/3 boxes): the under-six-hours build story, strategy first, context engineering from the $5 lesson, human directs and AI builds, with a "< 6 hrs" callout and a soft working-session invite (the single formal ask stays on the closing slide). Added to BOTH decks: presentation deck carries presenting guidance in notes, VIDEO deck carries a longer ~1:30 voiceover. Decks now 11 slides; leave-behind and ask shifted to 10 and 11; order verified evals to build to leave-behind to ask, slide re-rendered and QA'd. Content synced to deck-content-v2.md and deck-video-script.md. Per Oussama the repo is left as-is (campaign-strategy files remain in the pushed rbc-demo-repo); the on-slide repo reference is kept generic so nothing sensitive is named. Next action: Oussama records the scene-7 demo clip and builds the Google Vids video from the VIDEO deck.
- **2026-07-12** — **Slide 9 reworked after feedback.** First pass was too sparse and the ask too soft; rebuilt as two cards with a direct navy ask bar. Then corrected the framing per Oussama: on this slide prompt engineering means the Claude chat he steered through structured back-and-forth until it produced the game plan that seeded the workspace (not the tool's prompts, which stay on the demo and evals slides); context engineering is the file workspace that plan became. Left card = Prompt engineering, right card = Context engineering, ask bar spans this build and his other projects. Scene 9 voiceover reframed to match, now ~1:50. BUILD LESSON: a remove-then-add via python-pptx corrupted the ask slide (orphaned slide parts collided on duplicate names); recovered by regenerating from the clean base `outputs/Deck-v2.pptx` and only ADDING the slide. Both decks and both docs synced. Next action unchanged.
- **2026-07-12** — **Removed the standalone evals slide (was slide 8); decks now 10 slides.** Rationale (Oussama): the repo and its documentation already show the eval points (golden set, consistency, grounding, 3/3), the how-this-was-built slide now points to the repo, and the grounding proof stays on the demo slide. Flagged the trade-off (this was the campaign's stated top differentiator; a live viewer may not open the repo) and offered to fold a one-line eval proof into the demo slide (pending Oussama's word). Removed via safe pure-removal, no corruption, deck renders end-to-end. Also shipped a clean voiceover-only script `Video Script - AI Adoption - Oussama Hidane.md` (10 scenes) alongside the fuller `deck-video-script.md`. All docs synced. Order: title, solving-for, why-me, operating-model, posting-matched, first-90, demo, how-built, leave-behind, ask.
- **2026-07-12** — **Slide 3 reframed after a factual correction.** "Convinced skeptical, non-technical dealers to adopt a workflow / adoption is the job I just did" is NOT factual (venture pre-launch, no dealer has adopted). Replaced on-slide and in video scene 3 with the true, stronger story: translating one complex system for very different audiences (dealership sales and GMs, OEM captive risk/compliance/legal, VC and angel investors), customer discovery (pitched, assessed interest, pivoted), and the headline Oussama wants landed, teaching himself to use AI proficiently the hard way in real time while building everything ("learning to fly while flying"). Slide 3 bullet B is now "AI, learned the hard way, in real time." Both decks + both script files + deck-content docs + memory synced. FLAGGED: the same claim, plus numbers (7+ dealerships pitched, 15+ meetings, 4 converted to qualified partners), still lives in the resume and cover letter; awaiting Oussama's confirmation of those numbers before editing. Also flagged the six-agent pipeline / live-ERP lines for a factual re-check.
- **2026-07-12** — **Slide 4 now foregrounds the key lesson.** Reframed the intro line to "A founder's lesson: one teammate who hands you a decision beats ten who wait for a task," which sets up the existing loop and the "take work off your plate" punchline as the lesson embodied. Origin story (hiring a BD lead, wanting decision-ready proposals; the Apollo example where Oussama learned the platform himself and built the docs when it stalled) lives in the speaker notes for live delivery, framed around self-reliance and documentation. Video scene 4 voiceover uses a blame-free version (customer-facing) and grows to ~1:05. Both decks + both scripts + deck-content-v2 + memory synced. Framing rule recorded: never criticize the colleague on-slide or on-camera; keep it on what Oussama does.
- **2026-07-12** — Per Oussama, trimmed the slide 4 narration to the retained lesson only (dropped the BD-lead/Apollo backstory from the voiceover and the presentation notes). Scene 4 back to ~0:40. The Apollo example stays as a one-line optional live aid in the speaker notes. Decks + both scripts + content/memory synced.
- **2026-07-12** — **Slide 4 rebuilt into "The founder's state of mind."** Per Oussama, reframed from a generic operating loop to the mindset-as-asset argument: in an open-ended seat the analyst does not wait for tasks; as the AI expert he listens to the teams, spots the use case, scopes it (human and capital cost, benefit, implementation), and hands up a yes/no decision, taking "what should the AI analyst do each day" off the manager's plate. Four steps rewritten (Listen to the teams / Spot the use case / Scope it fully / Hand up a decision), proposal box reworked ("Every proposal carries"), title and intro changed, punchline kept. Brief positive VP-of-BD reference (wanted ownership) in voiceover and notes; Apollo backstory dropped. In-place text rebuild, rendered and QA'd. Scene 4 voiceover reframed, ~1:00. All decks, scripts, content and memory synced.
- **2026-07-12** — **Slide 7 framing + slide 8 ask broadened.** Slide 7 (demo) gains a subtitle, "A demonstration of 1.5 years building with AI, aimed at a real investment-team need," positioning the tool as the product of accumulated experience for the investment domain (behind-the-scenes / repo / live-meeting stays on slide 8 to avoid duplication). Slide 8 ask bar broadened to "here and across my work and school projects" (mirrors the JD's "academic or professional settings"). Scene 7 voiceover reframed with the 1.5-years opener. Presentation deck saved and QA'd; content + both script docs synced. The VIDEO deck was briefly file-locked (open on the machine / OneDrive), but saved cleanly on retry, so both decks are in sync.
- **2026-07-12** — **Analyzer upload upgraded to PDF/Word/PowerPoint with hard size limits.** Web UI (`web/public/`): the upload button now accepts .txt/.md/.pdf/.docx/.pptx; files convert to text IN THE BROWSER before anything reaches the API (only extracted text is ever sent). PDF via vendored pdf.js 6.1 (`public/vendor/`, self-hosted so the CSP stays `script-src 'self'`); .docx/.pptx parsed dependency-free with DecompressionStream + DOMParser (they are OOXML zips). Limits: 10 MB per file, extracted text trimmed to the 17k-char box with the existing warning, and the server cap in `functions/api/analyze.ts` lowered from 200k to 17k so the limit is enforced, not just client-side. Legacy .doc/.ppt get a clear "re-save as .docx/.pptx" error. Python engine: new `eca/convert.py` (stdlib zip+XML for Office files, pypdf for PDF, same 10 MB cap) + `python -m eca convert <file>` CLI (defaults to the 17k budget, `--max-chars 0` disables) and `ingest` now takes .docx/.pptx directly. Verified: mock analyze on a .docx (100% grounding), golden eval still 3/3 PASS, typecheck clean, tests 4/4, and in-browser upload of real docx/pptx/pdf fixtures through to a rendered report; oversize/legacy/server-limit guards all confirmed (413 over 17k). NOT deployed: changes are local, unpushed; deploy = git push (auto-deploy) after Oussama reviews. Next action: push, hard-refresh rbc.oussamahidane.com, and re-test uploads live.
