# RBC GAM Campaign — Workspace Instructions

Campaign to land the **AI Adoption Analyst** role at RBC Global Asset Management (Toronto).
**Submission cutoff: 11:59 PM ET, July 22, 2026.**

## File map — read in this order

1. `CLAUDE.md` (this file) — how to work in this workspace
2. `memory.md` — current state: locked decisions, workstream status, open items
3. `progress.md` — session log, append-only
4. `rbc-gam-game-plan.md` — full strategy; load only the sections the session's workstream needs

## Session protocol

1. Start from these files only — zero prior chat history. This lean-context discipline is itself a campaign proof point (the $5 token story).
2. Work one workstream per session where possible: **A** application, **B** relationship (mentee → hiring manager), **C** build (deck + earnings-call analyzer).
3. End every session: append one entry to `progress.md`, update `memory.md` (statuses, resolved open items).
4. Keep every file lean. Move completed detail to an archive rather than letting files bloat.

## Hard rules

- **Narrative frame is LOCKED** — use verbatim from `memory.md`. Never any version of "I need a job."
- Apply through the portal early regardless of the warm path. Sequence: confirm referral-logging order → submit → intro ask.
- Posture in all outward material: hypotheses offered for correction, never verdicts.
- Don't pitch sentiment analysis as novel. The edge is ship + document + evaluate + reproduce.
- No RBC logos or trademarks anywhere; RBC-adjacent styling only.

## Writing style

Direct, practical, zero corporate filler. Always explain why a decision was made, not just what it is.

- **No em dashes, ever.** Use commas, colons, periods.
- **No AI-telltale phrasing** in any outward material: no "not just X, it's Y", no "never the other way around", no "X isn't Y; it's Z", no rule-of-three flourishes, no "I'd welcome the conversation". Plain human sentences.
