{
  "name": "eca-web",
  "version": "0.1.0",
  "private": true,
  "description": "Web interface for the Earnings-Call Analyzer (Cloudflare Pages + Functions).",
  "scripts": {
    "dev": "wrangler pages dev",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/pipeline.test.ts",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.95.0"
  }
}
